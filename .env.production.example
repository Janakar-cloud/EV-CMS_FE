# ============================================
# PRODUCTION ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy these into your real production environment (Vercel / server env store).
# Do NOT commit any real secrets to Git.

# Main API Gateway URL for all REST API calls (via Nginx)
API_URL=https://www.fluxenergy.org/api/v1

# OCPP (Open Charge Point Protocol) Service URL
# Used for charger communication and monitoring (chargers/back-office tools)
# Backend: OCPP 1.6 protocol on port 8080
OCPP_API_URL=ws://3.108.74.82:8080/ocpp

# WebSocket base URL for real-time updates (Socket.IO)
# Socket.IO uses default path /socket.io on this origin
WS_URL=https://www.fluxenergy.org

# Google Maps API Key (REQUIRED - replace with real key in env store only)
GOOGLE_MAPS_API_KEY=REPLACE_WITH_PRODUCTION_GOOGLE_MAPS_KEY

# Sentry Error Tracking (OPTIONAL)
SENTRY_DSN=REPLACE_WITH_PRODUCTION_SENTRY_DSN
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1
SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# Application Metadata
APP_NAME=EV CMS Brand Admin
APP_VERSION=1.0.0

# Feature Flags
ENABLE_DEBUG_MODE=false
ENABLE_BETA_FEATURES=false

# Security Settings
CSP_ENABLED=true
HELMET_ENABLED=true

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Monitoring & Alerts
MONITORING_ENABLED=true
ALERT_EMAIL=admin@yourcompany.com

# Analytics (Optional)
ANALYTICS_ID=REPLACE_WITH_PRODUCTION_ANALYTICS_ID
